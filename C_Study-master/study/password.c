#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
#include <math.h>

int main ()
{
	int password [10] ;
	int a = 0;
    int b= 0;
    printf("Please enter your password ( It should have at least 1 uppercase letter, 1 number and a dollar sign($): \n"
	scanf("%d", &password);
	
	if( isupper (password[a]){
		a++;
        }
       if( isdigit (password[a]){
        b++;
        if( isalpha (password[a])){
            a++;
            if(a>6){
                printf(")
                       }
                       }
    }else if( isdigit (password[a]){
                
    }else
		printf("This password is not OK... /n");
	return 0;
}
